# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- master

pool:
  vmImage: 'ubuntu-latest'

steps:
- 
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'Conn1'
    subscriptionId: 'fb4c95c5-2774-4cbd-907e-d9685748ba89'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'armRSG02'
    location: 'South India'
    templateLocation: 'Linked artifact'
    csmFile: 'CreateWebApp/azuredeploy02.json'
    overrideParameters: '-projectName ARMtemplate -linkedTemplateUri https://raw.githubusercontent.com/PriyamvadaSharma/ARMtemplate/master/CreateWebApp/azuredeploy02.json'
    deploymentMode: 'Incremental'
    deploymentName: 'DeployPipelineTemplate'
    addSpnToEnvironment: true
script: echo Hello, world!
  displayName: 'Run a one-line script'

- script: |

  displayName: 'Run a multi-line script'
